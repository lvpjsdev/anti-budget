Implement the IndexedDB data layer for anti-budget app.

CREATE these files:

1. src/db/schema.ts — TypeScript interfaces:
   - Category: { id, name, emoji, createdAt }
   - Transaction: { id, categoryId, amount, note?, timestamp, createdAt }
   - Settings: { id: 'singleton', currency, unlocked, reminderEnabled, reminderTime, locale, createdAt, updatedAt }

2. src/db/index.ts — idb wrapper with:
   - setCategory() / getCategory() — one category only
   - addTransaction() / deleteTransaction() / getTransactionsByMonth() / getAllTransactions()
   - getSettings() / updateSettings() / setUnlocked()

3. src/hooks/useDb.ts — React hooks:
   - useCategory() — returns category, loading
   - useSettings() — returns settings, unlocked

4. src/main.tsx — initialize WebApp.ready() on mount

Make sure TypeScript compiles with 0 errors. Run: npx tsc --noEmit

After completion:
1. git checkout -b feature/db-layer
2. git add .
3. git commit -m "feat(db): IndexedDB data layer"
4. git push -u origin feature/db-layer
5. gh pr create --title "feat(db): IndexedDB data layer" --body "Task 002: IndexedDB schema and data layer"
6. gh pr merge (PR number) --squash --delete-branch