name = "anti-budget-stars"
compatibility_date = "2024-04-22"
main = "stars-worker.js"

# KV for premium storage
[[kv_namespaces]]
binding = "PREMIUM"
id = "0191a96b6e5f447c94ae20abca858207"

# Route for workers.dev (auto-registered on first deploy)
# After first deploy, your Worker URL will be:
# https://anti-budget-stars.<your-account>.workers.dev